- content_for :head do 
  \
= render 'projects/banner'
= breadcrumbs_for @news_letter
= render 'projects/sidebar'
.content
  %h1= @news_letter.name
  =image_tag @news_letter.image.url

  -if admin?
    .admin= link_to "Edit News Letter...", [:edit, @news_letter]